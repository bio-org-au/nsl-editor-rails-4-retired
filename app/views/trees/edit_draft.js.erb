debug('draft tree edit');
$('div#search-result-details').html("<%= escape_javascript(h(render partial: 'edit_draft')) %>");
<%= render("tree_form_cancel") %>
setTimeout(function () {
    debug('loading diff using loadHtml')
    loadHtml($('#diff-content'), "<%= @diff_link.html_safe %>");
}, 1000);
replaceDates();

