<%= form_for(@name_tag) do |f| %>
  <% if @name_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@name_tag.errors.count, "error") %> prevented this name tag from being saved:</h2>

      <ul>
      <% @name_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
