<h4>References, names, full synonymy examples</h4>
<table class="example-searches table table-striped">
  <% [
      {search_target: 'References, names, full synonymy', search_string: "1985 maslin", explanation: %Q(Searches for references, drills into all instances.)},
      ].each do |val| %>
  <tr>
    <td class="width-30-percent">
      <%= link_to(val[:search_string], search_path(query_string: val[:search_string], query_target: val[:search_target]),class:'blue') %>
    </td>
    <td><%= val[:explanation].html_safe %></td>
  </tr>
  <% end %>
</table>

