<br>
<% if @name.second_children.size > 0 %>
  <%= link_to("#{pluralize(@name.combined_children.size,'combined child')} #{icon('search')}".html_safe,
    search_path(query_string: "parent-or-second-parent-id: #{@name.id}",
                query_common_and_cultivar: "t",
                query_target: 'name'),
                tabindex: increment_tab_index,
                title: "Retrieve #{pluralize(@name.combined_children.size,'combined child')}.",
                class: '') %>
  <br>
<% end %>
<% if @name.children.size > 0 %>
  <%= link_to("#{pluralize(@name.children.size,'child')} #{icon('search')}".html_safe,
    search_path(query_string: "parent-id: #{@name.id}",
                query_common_and_cultivar: "t",
                query_target: 'name'),
                tabindex: increment_tab_index,
                title: "Retrieve #{pluralize(@name.children.size,'child')}.",
                class: '') %>
  <br>
<% end %>
<% if @name.second_children.size > 0 %>
  <%= link_to("#{pluralize(@name.second_children.size,'second parent child')} #{icon('search')}".html_safe,
    search_path(query_string: "second-parent-id: #{@name.id}",
                query_common_and_cultivar: "t",
                query_target: 'name'),
                tabindex: increment_tab_index,
                title: "Retrieve #{pluralize(@name.second_children.size,'second parent child')}.",
                class: '') %>
  <br>
<% end %>
