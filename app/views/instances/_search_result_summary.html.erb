  <%= %Q(#{pluralize(@search_results.count, "record") } \
          #{" (limited) " if @limited } \
          #{('<span class="search-info" title="'+@search_info+'">['.html_safe + @search_info.strip[0..40] + ']</span>'.html_safe) unless @search_info.blank? } \
          for #{link_to("#{params[:query]}",instances_search_path(query: params[:query]),
            tabindex: 35,
            class: 'rerun-query-link',
            data: {no_turbolink: true},
            title: 'Rerun this query.')}).html_safe if @search_results %><%= "(Ignored criteria: #{@rejected_pairings.join(',')})" if !@rejected_pairings.blank? %>

