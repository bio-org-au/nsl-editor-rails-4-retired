drop table bulk_name_raw;

create table bulk_name_raw (
  genus varchar,
  species varchar,
  subsp_var varchar,
  authority varchar,
  preferred_authority varchar,
  page varchar,
  page_extra varchar
);
